@model IEnumerable<FrontBerries.Models.AchievementsViewModel>

@{
    Layout = "_LayoutGame";
    ViewData["Title"] = "Achievements";
}

<h1 style="color: black; font-family:'Trebuchet MS',sans-serif; font-weight:bold;">Consult Achievements</h1>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tabla Estilizada</title>
    <style>
        .table {
            width: 100%;
            border-collapse: collapse; /* Elimina los espacios entre celdas */
            font-family: Arial, sans-serif; /* Define la fuente */
            margin: 20px 0; /* Espaciado alrededor de la tabla */
        }

            .table th, .table td {
                border: 1px solid #ddd; /* Borde alrededor de las celdas */
                padding: 8px; /* Espaciado dentro de las celdas */
                text-align: left; /* Alineación del texto */
            }

            .table th {
                background-color: gray; /* Fondo para los encabezados */
                font-weight: bold; /* Texto en negrita */
            }

            .table tr:nth-child(even) {
                background-color: lightgray; /* Color de fondo para filas pares */
            }

            .table tr:hover {
                background-color: darkgray; /* Color de fondo al pasar el ratón */
            }
    </style>
</head>

<table class="table" style="color: black;">
    <thead style="color: black; background-color: lightgray;">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdArchievement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameMission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameLevel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateAchievement)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdArchievement)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NameMission)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NameLevel)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateAchievement)
                </td>
                <td>
                    @Html.ActionLink("Update", "Update", new { id = item.IdArchievement }, new { style = "color: rebeccapurple;" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdArchievement }, new { style = "color: rebeccapurple;" })
                </td>
            </tr>
        }
    </tbody>
</table>
