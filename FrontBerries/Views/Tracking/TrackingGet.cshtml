@model IEnumerable<FrontBerries.Models.TrackingViewModel>

@{
    Layout = "_Layout";
    ViewData["Title"] = "Tracking";
}

<h1 style="color: black; font-family:'Trebuchet MS',sans-serif; font-weight:bold;">Consult Trackings</h1>
<p>
    <a asp-action="Create" style="color: rebeccapurple;">Create New</a>
</p>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tabla Estilizada</title>
    <style>
        .table {
            width: 100%;
            border-collapse: collapse; /* Elimina los espacios entre celdas */
            font-family: Arial, sans-serif; /* Define la fuente */
            margin: 20px 0; /* Espaciado alrededor de la tabla */
        }

            .table th, .table td {
                border: 1px solid #ddd; /* Borde alrededor de las celdas */
                padding: 8px; /* Espaciado dentro de las celdas */
                text-align: left; /* Alineación del texto */
            }

            .table th {
                background-color: gray; /* Fondo para los encabezados */
                font-weight: bold; /* Texto en negrita */
            }

            .table tr:nth-child(even) {
                background-color: lightgray; /* Color de fondo para filas pares */
            }

            .table tr:hover {
                background-color: darkgray; /* Color de fondo al pasar el ratón */
            }
    </style>
</head>

<table class="table" style="color: black;">
    <thead style="color: black; background-color: lightgray;">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTracking)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateTracking)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameState)
            </th>
            </td>
            </td>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdTracking)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DateTracking)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    @Html.ActionLink("Update", "Update", new { id = item.IdTracking }, new { style = "color: rebeccapurple;" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdTracking }, new { style = "color: rebeccapurple;" })
                </td>
            </tr>
        }
    </tbody>
</table>
